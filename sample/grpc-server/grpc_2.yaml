port: 8881     # for grpc service
http:
  port: 8882   # for http service
  gateway:
    path: "/test/"
client:
  address: "localhost:8883"  # for grpc client url and port

logging:
  level: debug # info | debug
  encoding: json # console | json
